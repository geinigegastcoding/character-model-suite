# Runs

Local and Modal run metadata/checkpoints belong here and are ignored by Git. Keep a small `run.json` beside every published adapter with the base-model revision, dataset hash, seed, and trainer commit.

