# Never commit real credentials. Modal Secrets are preferred for remote jobs.
HF_TOKEN=
HF_REPO_ID=geinigegastcoding/character-model-suite-adapters
WANDB_API_KEY=

# Used by the Modal entrypoint when you override the defaults locally.
SUITE_GPU=A100-80GB
SUITE_MODAL_ENV=dev
DIFFUSERS_REF=main

