stage = "coding"
model_id = "Qwen/Qwen3-Coder-30B-A3B-Instruct"
data_path = "/mnt/suite/data/manifests/coding.train.jsonl"
output_dir = "/mnt/suite/adapters/coding/qwen3-coder-30b-a3b"
method = "qlora"
lora_rank = 16
lora_alpha = 32
lora_dropout = 0.05
target_modules = "all-linear"
max_seq_length = 4096
per_device_train_batch_size = 1
gradient_accumulation_steps = 16
learning_rate = 0.0002
num_train_epochs = 3
logging_steps = 5
save_steps = 100
save_total_limit = 2
seed = 42
gradient_checkpointing = true
gpu = "L40S"
gpu_fallback = "A100-80GB"
status = "ready-to-wire"
notes = "Separate coding adapter. Start on L40S with short context; use the A100-80GB fallback if 4-bit activations OOM."
