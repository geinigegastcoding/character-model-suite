stage = "video"
model_id = "Wan-AI/Wan2.1-T2V-1.3B-Diffusers"
model_name = "wan"
trainer = "/opt/finetrainers/train.py"
dataset_config = "/root/project/configs/stages/video_training.json"
data_root = "/mnt/suite/data/raw/video"
output_dir = "/mnt/suite/adapters/video/wan21"
num_processes = 1
parallel_backend = "accelerate"
pp_degree = 1
dp_degree = 1
dp_shards = 1
cp_degree = 1
tp_degree = 1
dataset_shuffle_buffer_size = 10
enable_precomputation = true
precomputation_items = 8
precomputation_once = true
dataloader_num_workers = 0
flow_weighting_scheme = "logit_normal"
training_type = "lora"
seed = 42
batch_size = 1
train_steps = 1000
rank = 32
lora_alpha = 32
target_modules = "blocks.*(to_q|to_k|to_v|to_out.0)"
gradient_accumulation_steps = 1
gradient_checkpointing = true
checkpointing_steps = 250
checkpointing_limit = 2
enable_slicing = true
enable_tiling = true
optimizer = "adamw"
lr = 0.00005
lr_scheduler = "constant_with_warmup"
lr_warmup_steps = 100
lr_num_cycles = 1
beta1 = 0.9
beta2 = 0.99
weight_decay = 0.0001
epsilon = 0.00000001
max_grad_norm = 1.0
gpu = "A100-80GB"
status = "ready-to-wire"
notes = "Uses Hugging Face finetrainers Wan SFT/LoRA. Store each clip beside a matching .txt caption in the Modal Volume."
